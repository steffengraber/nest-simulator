[build-system]
requires = [
  "setuptools>=42",
  "wheel",
  "scikit-build",
  "Cython",
  "cmake",
  "pathlib",
  "ninja; platform_system!='Windows'",
  "gsl",
  "readline"
]
build-backend = "setuptools.build_meta"
